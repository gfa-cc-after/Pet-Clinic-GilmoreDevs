services:
  postgres-db:
    image: postgres:16
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: example

  backend:
    build: ./backend
    restart: always
    ports:
      - 8080:8080
    depends_on:
      - postgres-db
